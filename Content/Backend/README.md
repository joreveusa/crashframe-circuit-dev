# Backend

Blueprint Function Library for CrashFrame API calls.
- `BFL_CrashFrameAPI` — HTTP request helpers (login, loadout, match report)
- `DA_APIConfig` — server URL data asset (swap localhost ↔ prod)
